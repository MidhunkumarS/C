#include <stdio.h>
#include<string.h>
int main(void)
{
	char *a,temp;
        int n,i;
	scanf("%s",a);
	n=strlen(a);
	for(i=0;i<n;i=i+2)
	{
		if(a[i+1]!='\0')
		{
			temp=a[i];
			a[i]=a[i+1];
			a[i+1]=temp;
		}
		else 
		break;
}
printf("%s",a);
return 0;
}
